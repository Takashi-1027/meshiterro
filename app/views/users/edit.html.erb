<h2>プロフィール編集</h2>
<%= form_with model:@user, local:true do |f| %>  <!--form_withでは、さきほどのインスタンス（@user）を使う。-->
  <label for="inputName">Name</label>
  <%= f.text_field :name, autofocus: true, id:"inputName", placeholder:"名前"%>  <!-- text_fieldで、nameカラムを変更できるようにしている。 -->

  <!--プロフィール画像設定用にフォームを編集する機能 ここから-->
  <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg"%>  <!--attachment_image_tagは、画像を埋め込むときに使う。これにより、ユーザのプロフィール画像が表示される。-->
  <label for="inputImage">ProfileImage</label>
  <%= f.attachment_field :profile_image, placeholder: "プロフィール画像" %>  <!--attachment_fieldで、画像選択用のボタンが生成される。-->
  <!--ここまで-->

  <%= f.submit "変更を保存" %>
<% end %>